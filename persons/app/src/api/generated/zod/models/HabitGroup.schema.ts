/******************************************************************************
* This file was generated by ZenStack CLI.
******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
const baseSchema = z.object({
    id: z.string(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date().nullish(),
    isDeleted: z.boolean().default(false).nullish(),
    name: z.string(),
    sort: z.number().default(999).nullish(),
    color: z.string().nullish(),
}
).strict();
const relationSchema = z.object({
    children: z.array(z.unknown()).optional(),
}
);

/**
 * `HabitGroup` schema excluding foreign keys and relations.
 */
export const HabitGroupScalarSchema = baseSchema.omit({
    isDeleted: true,
});


/**
 * `HabitGroup` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const HabitGroupSchema = HabitGroupScalarSchema.merge(relationSchema.partial());


/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const HabitGroupPrismaCreateSchema = baseSchema.partial().passthrough();


/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const HabitGroupPrismaUpdateSchema = z.object({
    id: z.string(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date().nullish(),
    isDeleted: z.boolean().default(false).nullish(),
    name: z.string(),
    sort: z.union([z.number().default(999).nullish(), z.record(z.unknown())]),
    color: z.string().nullish()
}).partial().passthrough();


/**
 * `HabitGroup` schema for create operations excluding foreign keys and relations.
 */
export const HabitGroupCreateScalarSchema = baseSchema.partial({
    id: true, createdAt: true, updatedAt: true, isDeleted: true, sort: true
});


/**
 * `HabitGroup` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const HabitGroupCreateSchema = baseSchema.partial({
    id: true, createdAt: true, updatedAt: true, isDeleted: true, sort: true
});


/**
 * `HabitGroup` schema for update operations excluding foreign keys and relations.
 */
export const HabitGroupUpdateScalarSchema = baseSchema.partial();


/**
 * `HabitGroup` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const HabitGroupUpdateSchema = HabitGroupUpdateScalarSchema;

