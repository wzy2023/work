/******************************************************************************
* This file was generated by ZenStack CLI.
******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
const baseSchema = z.object({
    id: z.string(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date().nullish(),
    isDeleted: z.boolean().default(false).nullish(),
    name: z.string().nullish(),
    tags: z.any().nullish(),
}
).strict();

/**
 * `StudyItem` schema excluding foreign keys and relations.
 */
export const StudyItemScalarSchema = baseSchema.omit({
    isDeleted: true,
});


/**
 * `StudyItem` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const StudyItemSchema = StudyItemScalarSchema;


/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const StudyItemPrismaCreateSchema = baseSchema.partial().passthrough();


/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const StudyItemPrismaUpdateSchema = z.object({
    id: z.string(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date().nullish(),
    isDeleted: z.boolean().default(false).nullish(),
    name: z.string().nullish(),
    tags: z.any().nullish()
}).partial().passthrough();


/**
 * `StudyItem` schema for create operations excluding foreign keys and relations.
 */
export const StudyItemCreateScalarSchema = baseSchema.partial({
    id: true, createdAt: true, updatedAt: true, isDeleted: true
});


/**
 * `StudyItem` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const StudyItemCreateSchema = baseSchema.partial({
    id: true, createdAt: true, updatedAt: true, isDeleted: true
});


/**
 * `StudyItem` schema for update operations excluding foreign keys and relations.
 */
export const StudyItemUpdateScalarSchema = baseSchema.partial();


/**
 * `StudyItem` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const StudyItemUpdateSchema = StudyItemUpdateScalarSchema;

