/******************************************************************************
* This file was generated by ZenStack CLI.
******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
const baseSchema = z.object({
    id: z.number(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date().nullish(),
    isDeleted: z.boolean().default(false).nullish(),
    name: z.string(),
    sort: z.number().nullish(),
    count: z.any().nullish(),
    frequency: z.any().nullish(),
    enable: z.boolean().nullish(),
}
).strict();
const relationSchema = z.object({
    group: z.record(z.unknown()),
}
);
const fkSchema = z.object({
    groupId: z.number(),
}
);

/**
 * `HabitItem` schema excluding foreign keys and relations.
 */
export const HabitItemScalarSchema = baseSchema.omit({
    isDeleted: true,
});


/**
 * `HabitItem` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const HabitItemSchema = HabitItemScalarSchema.merge(fkSchema).merge(relationSchema.partial());


/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const HabitItemPrismaCreateSchema = baseSchema.partial().passthrough();


/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const HabitItemPrismaUpdateSchema = z.object({
    id: z.union([z.number(), z.record(z.unknown())]),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date().nullish(),
    isDeleted: z.boolean().default(false).nullish(),
    name: z.string(),
    sort: z.union([z.number().nullish(), z.record(z.unknown())]),
    count: z.any().nullish(),
    frequency: z.any().nullish(),
    enable: z.boolean().nullish()
}).partial().passthrough();


/**
 * `HabitItem` schema for create operations excluding foreign keys and relations.
 */
export const HabitItemCreateScalarSchema = baseSchema.partial({
    id: true, createdAt: true, updatedAt: true, isDeleted: true
});


/**
 * `HabitItem` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const HabitItemCreateSchema = HabitItemCreateScalarSchema.merge(fkSchema);


/**
 * `HabitItem` schema for update operations excluding foreign keys and relations.
 */
export const HabitItemUpdateScalarSchema = baseSchema.partial();


/**
 * `HabitItem` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const HabitItemUpdateSchema = HabitItemUpdateScalarSchema.merge(fkSchema.partial());

